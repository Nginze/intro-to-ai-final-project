from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Training a machine learning model to predict sentiment polarity in product reviews',
    author='Jonathan Kuug',
    license='',
)
